<p id="notice"><%= notice %></p>

<h1>Classes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @chatrooms.each do |chatroom| %>
      <tr>
        <td><%= chatroom.name %></td>
        <td><%= link_to 'Join', chatroom_chatroom_users_path(chatroom), method: :post %></td>
        <td><%= link_to 'Leave', chatroom_chatroom_users_path(chatroom), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Surveys', surveys_path %>
<!-- <br>
<%= link_to "Invite Me", new_user_invitation_path %>
<br>

<h5>Davet Gönderilen Kullanıcılar<h5>
<% User.all.each do |user| %>
<%= user.invitation_accepted? %>
  <div>
    <%= link_to user.email, edit_user_registration_path(user) %>
  </div>
<% end %> -->
